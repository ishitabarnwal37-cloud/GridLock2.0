# GridLock2.0
- $what\ is\ purpose\ of\ existence\ ?$
-  $is\ it\ to\ just\ stay\ in\ traffic\ ?$

## How to run the python file if I have not included the link : 

- install python 3.12 as the files are unstable for latest version
    ` py install 3.12`
    - Create the venv files : `python -m venv venv`
    - Activate the virtual enviroment for powershell : `.\venv\bin\Activate.ps1`
    - For cmd :`venv\Scripts\activate.bat`
    - Install libraries : `pip install -r requirements.txt`

